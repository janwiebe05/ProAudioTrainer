# Environment Configuration

# Backend JWT Secret (change this in production!)
JWT_SECRET=proaudio-secret-change-in-production

# Node Environment
NODE_ENV=production

# Backend Port (internal, exposed via Docker)
PORT=3001

# Frontend Port (exposed to host)
FRONTEND_PORT=8090

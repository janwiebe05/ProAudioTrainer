# Dependencies
node_modules/
.npm

# Rust build output (workspace + tauri) — never commit compiled artifacts
target/
src-tauri/target/

# Build output (electron-builder) — legacy, kept for branch parity
desktop/dist/

# Runtime data (persisted via Docker volume — never commit live data)
backend/src/data/users.json
backend/src/data/scores.json
backend/src/data/library.json

# Uploads
backend/uploads/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Tauri auto-generated capability schemas (regenerated on every build)
src-tauri/gen/schemas/
*.p12
